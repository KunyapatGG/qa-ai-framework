# Sources — <PROJECT NAME>

> Where this project's requirements live, so agents (via `shared/tools/`) know where to fetch.

## Jira
- **Project key(s):** `<PROJ>`
- **Boards / relevant JQL:** `<e.g. project = PROJ AND type = Story>`

## Confluence
- **Space key:** `<SPACE>`
- **Key pages:** <links to PRD / spec index>

## Git (optional — for traceability)
- **Repository:** `<repo url / path>`
- **Default base branch:** `<main>`

## Notes
- Access / permissions: <who has access, any restrictions>
